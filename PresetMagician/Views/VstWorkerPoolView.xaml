<catel:UserControl x:Class="PresetMagician.Views.VstWorkerPoolView"
                   xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                   xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                   xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                   xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                   xmlns:viewModels="clr-namespace:PresetMagician.ViewModels"
                   xmlns:catel="http://schemas.catelproject.com"
                   xmlns:controls="clr-namespace:PresetMagician.Controls"
                   d:DataContext="{d:DesignInstance viewModels:VstWorkerPoolViewModel}"
                   mc:Ignorable="d">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <GroupBox Header="Runnning Processes" Grid.Column="0">
            <controls:ProcessPoolGrid DataContext="{Binding ProcessPool.RunningProcesses}" />
        </GroupBox>
        <GroupBox Header="Terminated Processes" Grid.Column="1">
            <controls:ProcessPoolGrid DataContext="{Binding ProcessPool.OldProcesses}" />
        </GroupBox>

    </Grid>
</catel:UserControl>