<catel:UserControl x:Class="PresetMagicianShell.Views.SettingsView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:catel="http://schemas.catelproject.com"
      xmlns:local="clr-namespace:PresetMagicianShell.Views"
      xmlns:viewModels="clr-namespace:PresetMagicianShell.ViewModels"
      d:DataContext="{d:DesignInstance viewModels:SettingsViewModel}"
      mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />

        </Grid.ColumnDefinitions>
       
        <TabControl Grid.Column="0" TabStripPlacement="Bottom">
            <TabItem Header="General Settings">
                <Grid VerticalAlignment="Top">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Label Content="VST Folders" Grid.Row="0" Grid.Column="0"></Label>
                    <local:VstFolderList Margin="10" Grid.Column="1" Grid.Row="0" Height="300" DataContext="{Binding RuntimeConfiguration}"/>
                </Grid>
                
            </TabItem>
        </TabControl>
    </Grid>
</catel:UserControl>
